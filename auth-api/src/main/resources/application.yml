spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/winwin
    username: winwin
    password: winwin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
