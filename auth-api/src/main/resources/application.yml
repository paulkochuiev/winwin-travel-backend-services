spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/winwin
    username: winwin
    password: winwin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "VERY_LONG_SECRET_KEY_AT_LEAST_32_CHARS_LONG"
  expiration-ms: 3600000

server:
  port: 8080
